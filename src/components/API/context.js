import { createContext } from "react";

//create context object for "league id"

//set league as a property of every user object using the api league id

//set app league by getting it from user

//default league id is 2001
